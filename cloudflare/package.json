{
  "name": "outfitting-redirects",
  "version": "0.2",
  "private": true,
  "scripts": {
    "deploy": "pnpm run cf-typegen && wrangler deploy",
    "cf-typegen": "wrangler types",
    "dry-build": "pnpm run tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.9.6"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "wrangler": "^4.34.0"
  }
}
