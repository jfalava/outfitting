{
  "name": "outfitting-redirects",
  "version": "0.3",
  "private": true,
  "scripts": {
    "deploy": "bun run cf-typegen && bun run typecheck && bun run lint && bun run format && wrangler deploy",
    "cf-typegen": "wrangler types",
    "typecheck": "tsgo --noEmit",
    "lint": "oxlint --fix --type-aware .",
    "format": "oxfmt",
    "dev": "bun run cf-typegen && wrangler dev"
  },
  "dependencies": {
    "hono": "^4.11.0"
  },
  "devDependencies": {
    "@typescript/native-preview": "^7.0.0-dev.20251214.1",
    "oxfmt": "^0.16.0",
    "oxlint": "^1.32.0",
    "oxlint-tsgolint": "^0.8.6",
    "typescript": "^5.9.3",
    "wrangler": "^4.54.0"
  }
}
