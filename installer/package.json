{
  "name": "@outfitting/installer",
  "version": "0.10.0",
  "private": true,
  "scripts": {
    "cf-typegen": "wrangler types",
    "check": "tsgo --noEmit && oxlint --fix",
    "deploy": "wrangler types && tsgo --noEmit && oxlint && oxfmt && wrangler deploy",
    "dev": "bun run cf-typegen && wrangler dev",
    "format": "oxfmt",
    "lint": "oxlint",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "4.12.28"
  },
  "devDependencies": {
    "oxfmt": "catalog:build",
    "oxlint": "catalog:build",
    "oxlint-tsgolint": "catalog:build",
    "typescript": "catalog:build",
    "wrangler": "catalog:build"
  }
}
