---
title: Dotfiles and machine state
description: What lands on machines and how installers and sync commands reconcile it.
sidebar:
  order: 6
---

Desired state lives in the repo; machine-local operations and locks are pushed to the API worker. Platform install and maintenance pages cover the steps — this page maps the pieces.

## Dotfiles

- `dotfiles/Microsoft.PowerShell_profile.ps1` — Windows shell: `PATH` and the `outfitting-manager` command.
- `dotfiles/.gitconfig` — Windows Git identity and SSH commit/tag signing.
- `dotfiles/ssh.config` — shared GitHub SSH defaults (`jfalava-gitAuth-elliptic`, legacy `id-ed25519` fallback, `AddKeysToAgent`), included by the common Home Manager module on macOS, Linux, and NixOS. The user-owned `~/.ssh/config` remains editable.

Installers symlink the profile entry points (Windows backs up existing profiles with a timestamp); Apple Silicon, Linux, and NixOS machines link the nix-darwin / Home Manager sources instead.

## System definitions

- `system/macos/` (`darwin.nix`, `home.nix`, `flake.nix`, `zsh/macos.plugin.zsh`) — canonical `outfitting-manager update nix|brew|all` and `outfitting-manager snapshot brew`/`outfitting-manager recover nix` commands, `hm-build|switch|test|dry` compatibility helpers, Nix actions, Homebrew reconcile, remote Nix lock pull/push, and recovery checkpoints under XDG state. Global Bun updates use `bun update -g`.
- `system/ubuntu-wsl/` (`base.nix`, `flake.nix`, `zsh/wsl.plugin.zsh`) — Linux Home Manager profile; `hm-*` via shared `hm-profile.inc.zsh`.
- `system/oci-agents/` (`home.nix`, `flake.nix`, `bootstrap.sh`, `zsh/oci-agents.plugin.zsh`) — headless Linux Home Manager profile and services.
- `system/nixos/` (`configuration.nix`, `desktop.nix`, `server.nix`, `home.nix`, `flake.nix`) — host roles.
- `system/windows/registry/*.reg` — machine tweaks (Recall, Copilot, Ink, developer mode, menu delay).
- `system/common/` (`dotfiles.nix`, `zsh.nix`, `zsh/outfitting.plugin.zsh`, `zsh/hm-profile.inc.zsh`) — shared additive SSH, shell setup, and Linux `hm-*` helpers. Unix Git identity and signing are rendered by `packages/common/programs.nix`.

## Package manifests

Committed desired state: `packages/macos/` (Brewfile, nix), `packages/windows/` (`base`, `dev`, `work`, `gaming`, `qol`, `network`, `scoop.txt`, `pwsh-modules.txt`), `packages/linux/` (`generic-linux.txt`, `oci-agents.txt`), `packages/ubuntu-wsl/` (`apt.txt`, `packages.nix`), and `packages/common/`. Regular Windows profiles are canonical. Their manifests use `msstore:<id>` for Microsoft Store entries, which the CLI installs from the Store source. Windows clients fetch selected files from GitHub and keep the unified `windows.lock.json` outside the checkout.

## Fonts

`fonts/fontget.txt` lists the 16 public FontGet entries installed by post-install. Licensed fonts ship separately as R2 `releases/fonts.tar.gz` — see [CLI fonts](/docs/cli/fonts).

## Install and reconcile

Root `*-install-script.{sh,ps1}` files are served live from the installer hosts. Windows bootstraps the checksum-verified `outfitting-manager` release, fetches the selected manifests and PowerShell profile sparsely from GitHub, and symlinks profile entry points to the cached profile. Registry/OpenSSH setup remains PowerShell; post-install adds Scoop and fonts (with package installs delegated to the CLI).

Day-to-day Windows reconciliation uses `outfitting-manager sync` and uploads `windows.lock.json` (`<username>:x86_64-windows`). Direct remote transport is available under `outfitting-manager lockfiles`. macOS continues to use `nix` plus `homebrew-inventory` (`jfalava:aarch64-darwin`).
