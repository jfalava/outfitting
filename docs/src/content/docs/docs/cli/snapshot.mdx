---
title: snapshot
description: Capture observed Homebrew inventory on macOS.
audience: macos
sidebar:
  order: 110
---

`snapshot` is available on macOS and records observed package state without reconciling it against package declarations.

## `brew`

**Syntax:** `outfitting-manager snapshot brew`

Captures and pushes the observed Homebrew inventory.
