---
title: Init
description: Prepare local state and refresh the configured source.
sidebar:
  order: 9
---

`init` prepares runtime state, refreshes a configured remote source by default, and validates the selected source declared in [config.toml](/docs/source-configuration/). It does not install, upgrade, remove, or activate packages. Use [`validate`](/docs/cli/validate/) later to check config and the cached source without refreshing it.

```bash
outfitting-manager init
outfitting-manager --config ~/machine/config.toml init
```

Set `[source] path` for a local checkout, or `repository` and `ref` for remote Git. The selected platform profile comes from `[linux] profile`, `[macos] profile`, or `[windows] profiles`. Remote `--no-refresh` reuses an existing validated `source/` snapshot; local sources are read directly.
