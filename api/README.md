# Outfitting API

Cloudflare Worker that stores generated, machine-local lock state for `outfitting-manager`.

Deployed via the monorepo `iac/` Alchemy stack behind the edge router at `https://outfitting.jfa.dev/api`.

[Read the API documentation](https://outfitting.jfa.dev/docs/manager/api).
