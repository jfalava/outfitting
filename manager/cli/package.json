{
  "name": "@outfitting/manager",
  "version": "0.1.0",
  "private": true,
  "bin": {
    "outfitting-manager": "index.ts"
  },
  "type": "module",
  "module": "index.ts",
  "scripts": {
    "check": "bun run typecheck && bun run lint --fix && bun run format --check",
    "format": "oxfmt",
    "lint": "oxlint",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "catalog:build",
    "oxfmt": "catalog:build",
    "oxlint": "catalog:build",
    "oxlint-tsgolint": "catalog:build",
    "typescript": "catalog:build"
  }
}
