# Outfitting manager

The `outfitting-manager` binary is the entry point for local Outfitting
maintenance tools.

```bash
bun install
bun link
```

Run it without linking:

```bash
bun run manager/index.ts --help
```

Lockfile storage commands are documented in
[`packages/lockfiles/README.md`](../packages/lockfiles/README.md).
